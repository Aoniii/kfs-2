/* String helpers (freestanding libk). Standard prototypes. */
#ifndef KFS_STR_H
#define KFS_STR_H

#include "types.h"

int     strcmp(const char *s1, const char *s2);
size_t  strlen(const char *s);

#endif /* KFS_STR_H */
